import XCTest
@testable import TinkoffItsPackage

final class TinkoffItsPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TinkoffItsPackage().text, "Hello, World!")
    }
}
